#!/usr/bin/env ruby

require 'spec_helper'

describe Searchcreds do
    it 'has a version number' do
        expect(Searchcreds::VERSION).not_to be nil
    end

    it 'does something useful' do
        expect(false).to eq(true)
    end
end
